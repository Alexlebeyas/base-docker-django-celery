commands:
  00_create_dir:
    command: "mkdir -p /var/log/app-logs"
  01_change_permissions:
    command: "chmod -R 644 /var/log/app-logs"
  02_change_owner:
    command: "chown -R wsgi:wsgi /var/log/app-logs"
